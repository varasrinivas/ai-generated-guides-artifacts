# Copy to ~/.codex/boot4-loop.config.toml, then: codex --profile boot4-loop
# Phases 1-5: the agentic loop's daily driver.
model                  = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode           = "workspace-write"
approval_policy        = "never"
