# Copy to ~/.codex/boot4-review.config.toml, then: codex --profile boot4-review
# Security DSL rewrites and any data-affecting configuration change.
model                  = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode           = "workspace-write"
approval_policy        = "untrusted"
